<h1>トップページ</h1>
<%= link_to '新規投稿', '/lists/new' %>
<% @lists.each do |list| %>
  <div class="list">
    <div class="list-date">
      投稿日時：<%= list.created_at %>
    </div>
    <div class="list-memo">
      <%= list.memo %>
    </div>
  </div>
<% end %>
